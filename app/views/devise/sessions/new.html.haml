= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-signup text-center"}) do |f|
  = image_tag 'logo-small.png'
  %h1.h3.mt-2.pb-2.font-weight-normal Вход
  = f.label :email, class: 'sr-only'
  = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email", class: "form-control field_top"
  = f.label :password, 'Пароль', class: 'sr-only'
  = f.password_field :password, autocomplete: "current-password", placeholder: "Пароль", class: "form-control field_bottom"
  - if devise_mapping.rememberable?
    .form-check
      = f.check_box :remember_me
      = f.label "Запомнить меня"
  = f.submit "Войти", class: "mt-2 btn btn-lg btn-primary btn-block"
  %p.mt-2
    Вы также можете #{link_to 'зарегистрироваться', new_user_registration_path}.
  = render "devise/shared/links"
