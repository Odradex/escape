%h2 Забыли пароль?
= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "edit-user"}) do |f|
  = render "devise/shared/error_messages", resource: resource
  .form-field
    = f.label :email, 'Адрес электронной почты'
    %br/
    = f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control"
  .actions
    = f.submit "Отправить инструкции по сбросу пароля", class: "btn btn-primary mt-3 mb-3"
= render "devise/shared/links"
